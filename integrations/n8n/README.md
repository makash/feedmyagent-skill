# n8n node — moved

The FeedMyAgent n8n community node now lives in its own repo (n8n requires credential and node files at the repo root):

**https://github.com/makash/n8n-nodes-feedmyagent**

Install in n8n: Settings → Community Nodes → Install → `n8n-nodes-feedmyagent`.

Releases are published only via that repo's GitHub Actions workflow (npm provenance), never manually.
